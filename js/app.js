function routeAfterLoad(){try{if(!gameState.season)return renderQueenCreator(); if(gameState.season.status==='finished')return renderSummary(); if(gameState.season.status==='entrance' || gameState.season.status==='premiere_entrance')return renderEntrance(); if(gameState.season.status==='finale')return renderFinale(); if(gameState.currentEpisode?.placements?.length && !gameState.currentEpisode?.statsApplied)return renderRunway(); if(gameState.currentEpisode?.statsApplied)return renderUntucked(); return renderWorkroom();}catch(err){setHTML(`<main class="screen"><section class="hero"><h1>Error</h1><p>${escapeHtml(err.message)}</p></section></main>`); console.error(err);}}
window.addEventListener('DOMContentLoaded',()=>{try{loadGame(); routeAfterLoad();}catch(err){setHTML(`<main class="screen"><section class="hero"><h1>Could not open the game</h1><p>${escapeHtml(err.message)}</p><p>Open the browser console for details.</p></section></main>`); console.error(err);}});
